function iniciar() {
  alert("Bem Vindo");
}

function somar() {
  res.value = parseInt(v1.value) + parseInt(v2.value); 
}

function subt() {
  res.value = parseInt(v1.value) - parseInt(v2.value); 
}

function mult() {
  res.value = parseInt(v1.value) * parseInt(v2.value); 
}

function div() {
  res.value = parseInt(v1.value) / parseInt(v2.value); 
}